import vendorTemplate from 'byome/utils/vendor-template';
import { module, test } from 'qunit';

module('Unit | Utility | vendor template');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = vendorTemplate();
  assert.ok(result);
});
