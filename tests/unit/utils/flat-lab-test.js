import flatLab from 'byome/utils/flat-lab';
import { module, test } from 'qunit';

module('Unit | Utility | flat lab');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = flatLab();
  assert.ok(result);
});
